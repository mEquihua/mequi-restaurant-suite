# Copy to .env for local Compose use and replace every placeholder before deployment.
TRAEFIK_HTTP_PORT=80
TRAEFIK_HTTPS_PORT=443
API_LOG_LEVEL=info
POSTGRES_DB=restaurant_suite
POSTGRES_USER=restaurant_suite
POSTGRES_PASSWORD=replace-with-a-local-development-password
CLOUDFLARED_TUNNEL_TOKEN=replace-with-a-profile-gated-tunnel-token
