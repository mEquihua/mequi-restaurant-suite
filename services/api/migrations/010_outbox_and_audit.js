export const up = (pgm) => {
  pgm.sql(`
CREATE TABLE outbox_events (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), location_id UUID NOT NULL REFERENCES locations(id), aggregate_type VARCHAR NOT NULL, aggregate_id UUID NOT NULL, event_type VARCHAR NOT NULL, payload JSONB NOT NULL, schema_version INTEGER NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), dispatched_at TIMESTAMPTZ); CREATE INDEX idx_outbox_events_location_id ON outbox_events(location_id); CREATE TRIGGER set_updated_at_outbox_events BEFORE UPDATE ON outbox_events FOR EACH ROW EXECUTE FUNCTION set_updated_at();
CREATE TABLE audit_events (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), location_id UUID NOT NULL REFERENCES locations(id), actor_id UUID NOT NULL, terminal_id UUID NOT NULL, action VARCHAR NOT NULL, aggregate_type VARCHAR NOT NULL, aggregate_id UUID NOT NULL, before_version INTEGER, after_version INTEGER, reason VARCHAR, request_id VARCHAR NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()); CREATE INDEX idx_audit_events_location_id ON audit_events(location_id); CREATE TRIGGER set_updated_at_audit_events BEFORE UPDATE ON audit_events FOR EACH ROW EXECUTE FUNCTION set_updated_at();
CREATE TABLE command_idempotency (location_id UUID NOT NULL REFERENCES locations(id), command VARCHAR NOT NULL, idempotency_key VARCHAR NOT NULL, payload_hash CHAR(64) NOT NULL, response JSONB NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), PRIMARY KEY (location_id, command, idempotency_key));
`);
};
